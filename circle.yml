machine:
  node:
    version: 7

dependencies:
  override:
    - "true"

test:
  override:
    - "true"
    - npm install @angular/cli@latest -g

deployment:
  prod:
    branch: master
    commands:
      - ng build --prod
      - aws s3 sync dist/ s3://lab.igniterbee.com --delete