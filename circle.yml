dependencies:
  pre:
    #- npm install npm@latest -g # Should fix bug in ng test
    #- npm install @angular/cli@latest -g

test:
  override:
    - "true"

deployment:
  prod:
    branch: master
    commands:
      - ng build --prod
      - aws s3 sync dist/ s3://lab.igniterbee.com --delete